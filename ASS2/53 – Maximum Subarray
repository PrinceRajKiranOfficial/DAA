class Solution {
    public int maxSubArray(int[] nums) {

        // current = maximum sum ending at current position.
        int current = nums[0];

        // maximum = maximum subarray sum found so far.
        int maximum = nums[0];

        for (int i = 1; i < nums.length; i++) {

            // Either start a new subarray from nums[i]
            // or continue the previous subarray.
            current = Math.max(nums[i], current + nums[i]);

            // Update the overall maximum.
            maximum = Math.max(maximum, current);
        }

        return maximum;
    }
}

// This algorithm is called Kadane's Algorithm.
// Time Complexity: O(n)
// Space Complexity: O(1)
